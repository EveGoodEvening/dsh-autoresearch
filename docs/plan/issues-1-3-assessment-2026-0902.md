# GitHub Issues #1–#3: Scoped Assessment and Remediation Tracker

## Authority and scope

This file is the sole live plan and sequential checklist for GitHub issues [#1](https://github.com/EveGoodEvening/dsh-autoresearch/issues/1), [#2](https://github.com/EveGoodEvening/dsh-autoresearch/issues/2), and [#3](https://github.com/EveGoodEvening/dsh-autoresearch/issues/3).

The issue body’s demonstrated defect and explicit remedies define each closure boundary. A broader security or reproducibility concern is recorded separately and does not block an issue unless the issue text makes it part of the required boundary. Conversely, closure may not omit an explicit issue requirement.

This is a planning artifact only. It changes no source or tests, runs no verification gate, posts no issue comment, and changes no issue state. Checked historical evidence below remains evidence of work previously observed, not fresh proof for closing these issues. Every new implementation, verification, review, accounting, comment, and closure step starts unchecked.

`docs/plan/handoff-2026-0830-checklist.md` remains the historical authority for its completed H-01 through H-15 remediation. This tracker neither rewrites nor reopens that history.

## Initial GitHub state baseline

Observed by unauthenticated direct read of each canonical GitHub issue page at **2026-09-02T13:01:32Z**. The rendered pages exposed the open state but no separate state reason. Later read-backs supersede these observations for current state while preserving this baseline historically.

| Issue | Canonical URL | Observed state | State reason | Evidence source |
|---|---|---|---|---|
| #1 | https://github.com/EveGoodEvening/dsh-autoresearch/issues/1 | open | not exposed | canonical issue page, direct read |
| #2 | https://github.com/EveGoodEvening/dsh-autoresearch/issues/2 | open | not exposed | canonical issue page, direct read |
| #3 | https://github.com/EveGoodEvening/dsh-autoresearch/issues/3 | open | not exposed | canonical issue page, direct read |

## Scoped dispositions

| Issue | Exact defect and required remedy | Current assessment | Closure boundary |
|---|---|---|---|
| #1 | A model-selected target repository can escape the parent Agent repository/workspace. Default to the canonical parent repository or canonical containment within the parent workspace. Cross-repository operation must require separate human command/approval or deployment-controlled allowlisting; the same model call may not both select and authorize the other repository. | Technically complete, freshly verified, and cleanly reviewed on the current HEAD. Canonical repository/workspace containment and the full Chunk 01 corrective sequence are present; aggregate and post-fix/completeness reviews are clean, and the issue-specific Chinese closure comment was independently reviewed and approved. The issue remains open solely because GitHub authentication was unavailable, so no external comment, close, or post-action read-back occurred. | Close after canonical containment is enforced before target repository discovery or any mutation/effect; the accepted same-repository/contained-workspace rule is explicit; lexical and canonical escapes are rejected; cross-repository authority is separately human/deployment-owned or cross-repository operation is rejected; fresh start/resume and foreground/background evidence covers root, contained workspace, `..`, symlink, nested repository, sibling, external repository, linked worktree, and canonical aliases; the implementation/accounting range passes aggregate current-tree verification and review; and the issue-specific comment/read-back, closure, post-action read-back, and terminal accounting gates complete. |
| #2 | Model-provided evaluator command, args, and environment reached Host subprocess authority. Establish a non-model trust boundary through deployment-pre-registered evaluator IDs or a human-created immutable run manifest; if raw argv remains model-facing, use the issue’s sandbox + approval + allowlist alternative. | Technically complete, freshly verified, and cleanly reviewed on the current HEAD. Host-owned evaluator registration, removed raw model authority, durable registration identity, mismatch handling, and the complete lifecycle drift matrix are present; aggregate and completeness reviews are clean, and the issue-specific Chinese closure comment was independently reviewed and approved. The issue remains open because external processing stopped sequentially at unauthenticated issue #1; #2 was not attempted. | Close when fresh evidence proves evaluator selection and exact argv/environment are immutable Host/human-owned data unavailable to model modification across start, resume, and recovery, with pre-effect rejection of missing, unknown, submitted-on-resume, changed, or mismatched identity. Hostile candidate isolation is separate and non-blocking. |
| #3 | Declared evaluator and dataset files were not actually frozen: their identity/SHA-256 was absent from the production boundary, not revalidated before every spawn, and not automatically excluded from mutation. | Technically complete, freshly verified, and cleanly reviewed on the current HEAD. Exact declared evaluator/local-dataset identity and SHA-256, automatic mutable-surface exclusion, lifecycle revalidation, the corrected immediate pre/post-provider-spawn boundary, and exact scoped README wording are present; aggregate and completeness reviews are clean, and the issue-specific Chinese closure comment was independently reviewed and approved. The issue remains open because external processing stopped sequentially at unauthenticated issue #1; #3 was not attempted. | Close when fresh evidence proves exact declared evaluator/local-dataset file identity and SHA-256 are recorded before baseline, excluded without filename heuristics, and revalidated before baseline, candidate, resume, and recovery spawns, rejecting mismatch before spawn. Runtime executable/dependency closure is separate and non-blocking. |

## Preserved completed remediation evidence

The following is historical evidence from `docs/plan/handoff-2026-0830-checklist.md`; it must be cited accurately but must not be treated as a substitute for fresh closure verification:

- **Chronology — preparation:** `174ecb01d2834398c9b0c2496c0f53a679ed5730` prepared durable evaluator registration identity; accounting `efaabef4da68387c417371a4a8611f2b6bb60667`.
- **Chronology — preparation:** `75949d0fe7aa504537e98b5941e14f7da364279e` prepared declared evaluator/dataset hashing, exact-path protection, and revalidation primitives; accounting `b2a7f14d2e3f8bdaa375d659a02abbb50564db28`.
- **Chronology — preparation:** `cf3d0c33d24902fb558500dfcd176d23586c97a1` prepared the Host evaluator registry and model contract; accounting `488c73a1edb43671730ca8dbe5f257f8f8c461db`.
- **Chronology — hardening:** `d9c10369cdc83948dd6cd0960fe2d63caf1d2fac` plus review fixes `da87f21276a83f842cee15f6870818caada71751`, `aca459bb94a895078cda855f64c8fde5e7e356e5`, `653add91657902e3e4889796da510dbdb1d191b9`, and `9d6b3ca503fa27256095765a1dbdc807188c6689` hardened activation primitives; accounting `5df192445a6d2da96958590d3d4c00d8f1b015a9` and `ed849ad18279e14e54ac69072b6e32b5873e3df9`.
- **Chronology — activation:** `4b9e93efcb343b9a9cf017ba47605dc44e867e6a` activated Host-owned evaluator selection and frozen evaluator/data identity; accounting `0dee00d206dd4414c4034255c0affd5a2050b730`.
- **Chronology — documentation predecessor:** `aec50bc625d1635297f83643d00834adfb194744` aligned README trust and evaluator/data wording with the implemented boundary.
- **Chronology — subsequent boundary fix:** `f511456436d2067a522e8e001f49dd8bdeb13603` fixed the typed frozen-boundary gap; its recorded focused suite passed 301 tests and typecheck, followed by clean review; accounting `6cbe16c0a702c2976a4d5401d3d0fdf34389cfdc` (`docs(plan): record handoff chunk 08`).

Fresh verification must exercise the current tree, current public contract, all named lifecycle paths, and every issue-specific negative case. A fresh finding reopens only its owning scoped chunk and any truly dependent later step.

## Separately recorded, non-blocking concerns

These concerns may warrant separate issues, but they are not closure prerequisites for #1–#3:

1. **Broader effect authority and delegation:** cwd/repository containment is not a complete grant for Git, SQLite, artifacts, child Agents, subprocesses, or cleanup. Track separately; do not convert it into an external-capability blocker for ordinary same-repository #1 closure.
2. **Hostile candidate isolation:** a Host-selected evaluator may execute or import candidate-controlled code without complete filesystem/process/network/credential isolation. Track separately; do not redefine #2 after evaluator argv/environment selection is Host/human-owned.
3. **Runtime dependency reproducibility:** executable, interpreter, shared-library, external module, provider, and image identities may not be content-bound. Track separately; do not expand #3 beyond the declared evaluator/dataset files named by the issue.
4. **External dataset identity:** remote or otherwise repository-unavailable dataset bytes may need an immutable algorithm-qualified digest contract. Track separately; this is not verification, remediation, or closure work for #3, which is limited to the declared evaluator and dataset files required by the issue.

Any follow-on record must clearly say it is non-blocking for #1–#3 and must not be cited as a reason to withhold closure after the scoped contracts pass.

## Durable accounting checkpoint protocol

An accounting checkbox may assert only facts observable before its commit: that the record is complete and its staged diff was reviewed. It must never claim that its own commit has already landed. The reviewed tracker-only accounting commit is itself the durable checkpoint; its resulting SHA is recorded in the next chunk's accounting record before that later work is accounted. For the terminal accounting commit, Git history plus its unique terminal commit subject is the durable locator, while the committed tracker content carries the final canonical state and any resume pointer. If later tracker work occurs, that next commit records the terminal accounting SHA. No follow-up commit exists solely to make a self-referential checkbox true.

## Minimal sequential plan

### Chunk 00 — Land and account for this scoped assessment

- **Path:** only `docs/plan/issues-1-3-assessment-2026-0902.md`.
- **Work:** replace the obsolete unconditional activation shutdown, external Host effect-grant dependency, full isolation-provider dependency, runtime/image identity dependency, and their expanded chunks/blockers with this scoped plan; preserve the initial GitHub baseline and historical remediation evidence.
- **Planning review:** compare the staged diff with all three issue bodies and `local://issues-plan-scope-arbitration.json`; confirm every arbitration decision is implemented, future steps remain unchecked, and no source, test, README, historical tracker, issue comment, or issue state changed.
- **Planning commit:** `4aa1cdadd0263ca57db16309df90af8bd911fb6e` (`docs(plan): assess issues 1 through 3`).
- **Changed-path proof:** the planning commit added only `docs/plan/issues-1-3-assessment-2026-0902.md`, with 158 insertions and no source, test, README, historical-tracker, issue-comment, or issue-state change.
- **Planning review result:** clean; two final reviewers compared the scoped assessment with all three issue bodies and the approved arbitration, and returned zero findings. The review confirmed that #1 is limited to canonical parent repository/workspace containment with separately authorized or rejected cross-repository operation; #2 retains the Host/human-owned evaluator authority boundary without adding hostile-candidate isolation or metric-definition ownership; #3 retains declared evaluator/dataset file identity and SHA-256 without adding runtime/image or external-dataset identity; and every later implementation, verification, issue action, and closure item remains unchecked.
- **Initial-state evidence:** the planning commit preserves the unauthenticated canonical-page baseline observed at `2026-09-02T13:01:32Z`: issues #1, #2, and #3 were open and no separate state reason was exposed.
- **Rollback:** revert only `4aa1cdadd0263ca57db16309df90af8bd911fb6e` and this tracker-only accounting commit before Chunk 01 begins. After dependent work begins, preserve the scoped arbitration and roll forward rather than restoring the obsolete expanded blockers.
- **Dependency-ready next chunk:** `resume at Chunk 01 — canonicalize and compare the requested target before repository discovery`.
- **Exact Chunk 01 targets:** `src/controller.ts`, `src/git.ts`, `src/types.ts`, `tests/git.spec.ts`, `tests/controller.spec.ts`, and `tests/composition.integration.spec.ts`; no other path is in the dependency-ready slice unless the implementation proves a direct caller or public-contract dependency.
- **Acceptance:** derive the canonical allowed parent repository/workspace independently of model input; canonicalize and compare every requested start/resume target before repository discovery, retention, tracker access, Git/worktree/ref mutation, job/Agent creation, evaluator spawn, artifacts, or cleanup; allow the canonical parent repository and the plan's single documented contained-workspace rule; reject lexical, symlink, nested-repository, sibling, external-repository, linked-worktree, and canonical-alias escapes without effects; and reject cross-repository operation unless a separate non-model-owned human/deployment authority is implemented and durably preserved across resume.
- **Verification commands:** `pnpm exec vitest run tests/git.spec.ts tests/controller.spec.ts tests/composition.integration.spec.ts`; `pnpm run typecheck`.
- **Commit subjects:** implementation `fix(security): contain autoresearch repository targets`; tracker-only accounting `docs(plan): record issue 1 containment`.
- [x] Planning diff reviewed and planning commit landed.
- **Accounting review result:** the tracker-only diff was independently reviewed clean before `d9942da3a3f695b923c5113bc336554b190061f9` (`docs(plan): record issues 1 through 3 planning`) landed; that commit is the durable Chunk 00 accounting checkpoint carried into Chunk 01.
- [x] Accounting record complete and staged tracker-only diff reviewed for commit.

### Chunk 01 — Implement and freshly verify issue #1 repository containment

- **Issue owner:** #1 only.
- **Predecessor durable checkpoint:** `d9942da3a3f695b923c5113bc336554b190061f9` (`docs(plan): record issues 1 through 3 planning`) durably records the completed Chunk 00 planning/accounting state that precedes this implementation and corrective sequence.
- **Work:** derive the canonical parent repository/workspace boundary independently of the model-selected target; canonicalize the requested target before repository discovery, retention, tracker access, Git/worktree/ref mutation, job/Agent creation, evaluator spawn, artifacts, or cleanup. Permit ordinary operation when the target is the canonical parent repository or is canonically contained within the allowed parent workspace, according to the chosen single documented rule. Reject lexical and canonical escapes before effects.
- **Cross-repository rule:** the model-facing request alone can never authorize an external repository. Either reject all cross-repository targets or accept them only through a separately authenticated human command/approval or deployment-owned allowlist whose authority cannot be created, selected, or widened by the same model call. Same-repository operation has no external capability prerequisite.
- **Verification:** fresh start and resume; repository root and allowed contained workspace; `..`, symlink, nested repository, sibling, external repository, linked worktree, canonical alias, foreground, and background paths. Rejections must prove no discovery/mutation side effects. If a cross-repository authority route exists, verify allow, deny, provenance, model non-widening, and resume persistence.
- **Review:** issue-text scope, canonical-path correctness, check ordering, TOCTOU assumptions actually claimed, start/resume parity, and no accidental requirement for a broader effect-class grant.
- **Implementation commit:** `75b13a81cff24f44637d605e4e715cd09ca20bfe` (`fix(security): contain autoresearch repository targets`). The focused commit changed only `src/controller.ts`, `src/git.ts`, `src/types.ts`, `tests/git.spec.ts`, `tests/controller.spec.ts`, and `tests/composition.integration.spec.ts`; it introduced canonical target containment and the initial start/resume coverage.
- **Initial observed verification:** `pnpm run build` passed; focused `pnpm exec vitest run tests/git.spec.ts tests/controller.spec.ts tests/composition.integration.spec.ts` passed **208/208** tests; `pnpm run typecheck` passed.
- **Round-1 findings and correction:** review found that the background path registered a job before repository-target containment completed and that the linked-worktree test did not prove rejection before job creation. Corrective commit `e05740d0c559f484286d473986511f6bd718d83f` (`fix(security): preflight background repository targets`) changed `src/controller.ts`, `src/index.ts`, `tests/autoresearch.spec.ts`, `tests/composition.integration.spec.ts`, and `tests/git.spec.ts`; it moved canonical repository discovery into a reusable preflight completed before background job registration and passed the accepted result into the controller.
- **Round-2 findings:** re-review found three accounting or implementation gaps: an unknown background evaluator could still reach repository preflight before evaluator-ID rejection; model-provided `resume_run_id` was usable as a tracker path component, permitting path escape/tracker I/O and, on the background path, job-registration risk; and the prior LSP/accounting record omitted part of the changed TypeScript union and did not yet carry the complete corrective evidence.
- **Round-2 corrective commit:** `342535bfd845daf17a0b1c17f9086ed350b13d38` (`fix(security): contain resume tracker identity`) changed `src/controller.ts`, `src/index.ts`, `src/state-layout.ts`, `src/types.ts`, `tests/autoresearch.spec.ts`, `tests/contracts.spec.ts`, and `tests/controller.spec.ts`. It validates the evaluator selection or canonical UUID-v4 durable resume ID before foreground/background dispatch, validates the resume ID again at controller/tracker use, and resolves or inspects tracker state through contained, symlink-rejecting `StateLayout` traversal; the added contract/controller/background tests cover malformed and escaping resume identities, rejection before tracker I/O, and rejection before background job registration.
- **Penultimate review finding and correction:** review found that cancellation arriving after repository preflight resolved but before generic background job registration could still allow the job to be registered. Corrective commit `7931d4a03972c8e4c84aacb35c3a6993773130e4` (`fix(jobs): stop cancelled repository preflight`) changed only `src/index.ts` and `tests/autoresearch.spec.ts`; it rechecks the execution signal immediately after preflight and proves that a cancellation raised during preflight rejects with the cancellation reason before job registration or controller construction.
- **Final startup-cancellation finding and correction:** final review found that, after generic background job registration, cancellation while controller preparation or startup readiness remained pending was not raced against those waits, so startup could remain unsettled instead of promptly cancelling and completing the registered job. Corrective commit `c32ef8675a3e47a8672bebb1e5fd12b74ab58ada` (`fix(jobs): cancel pending background startup`) changed only `src/index.ts` and `tests/autoresearch.spec.ts`; it installs startup-abort handling after preflight, races cancellation against controller preparation and readiness, cancels the controller, releases the startup gate, returns a cancelled startup failure for a registered job, and proves the job hook settles as killed for cancellation during either pending stage while preserving rejection before registration for cancellation immediately after preflight.
- **Pending-prepare cancellation finding and correction:** the next re-review found that controller disposal still awaited a non-cooperative pending preparation promise, so cancellation could settle the registered background job while leaving cleanup blocked and any runtime produced later unreleased. Corrective commit `2dfb9aefab75383ba18dfe660966b0d2bd4f748a` (`fix(jobs): release cancelled preparation`) changed exactly `src/controller.ts`, `tests/autoresearch.spec.ts`, and `tests/controller.spec.ts`; disposal now rejects readiness and returns promptly without awaiting pending preparation, while a runtime that preparation produces after disposal is released and closed, and the focused tests prove prompt disposal under permanently pending preparation.
- **Registered-startup cancellation finding and correction:** the latest re-review found that generic job cancellation during registered, non-cooperative controller preparation did not settle the tool startup or registered job because the generic cancellation hook cancelled the controller without releasing the startup gate or rejecting the startup race. Corrective commit `32895e077da7b34b2099fb7c8aa884a1c15b865f` (`fix(jobs): settle cancelled background startup`) changed exactly `src/index.ts` and `tests/autoresearch.spec.ts`; generic cancellation now releases the gate, resolves cancelled startup readiness, and rejects the startup race, while controller cancellation remains idempotent. The focused tests prove both generic cancellation and outer abort promptly settle the tool result and registered job as cancelled/killed during non-cooperative preparation, never start the runner, dispose exactly once, and allow plugin disposal to settle.
- **Ordinary-startup-failure status finding and correction:** the latest review found that an ordinary controller preparation or other startup failure was unconditionally marked as cancellation in the catch path, causing the registered background job to settle as `killed` instead of `failed`. Corrective commit `6a14a7a250f93a710d536b9648c4a89a511d4208` (`fix(jobs): classify startup failures`) changed exactly `src/index.ts` and `tests/autoresearch.spec.ts`; it preserves cancellation classification only when cancellation or abort actually occurred, while ordinary rejecting preparation and partial-registration startup failures now settle the tool result and job hook as `failed`, still cancel and dispose the controller, and never start the runner.
- **Latest observed verification:** `pnpm run build` passed; exact command `pnpm exec vitest run tests/git.spec.ts tests/controller.spec.ts tests/composition.integration.spec.ts tests/autoresearch.spec.ts tests/contracts.spec.ts` passed **5 files / 305 tests (305/305)**; `pnpm run typecheck` passed.
- **LSP diagnostics:** unavailable because no language server was found for the full changed TypeScript union: `src/controller.ts`, `src/git.ts`, `src/index.ts`, `src/state-layout.ts`, `src/types.ts`, `tests/autoresearch.spec.ts`, `tests/composition.integration.spec.ts`, `tests/contracts.spec.ts`, `tests/controller.spec.ts`, and `tests/git.spec.ts`. This is an unavailable auxiliary signal, not a substitute for or contradiction of the observed build, typecheck, and focused-suite results.
- **Latest implementation review result:** clean; the latest issue-specific code review returned zero findings after the full corrective sequence, so the containment implementation and Chunk 01 review/fix loop are complete. Issue #1 remains open pending the tracker-only Chunk 01 accounting commit, later aggregate current-tree verification and review in Chunk 04, and the issue-specific GitHub comment, comment read-back, closure, post-action state read-back, and terminal accounting actions.
- **Compatibility and rejection ordering:** ordinary foreground and background starts retain the existing repository-root and canonically contained-workspace behavior, evaluator IDs retain the Host registry contract, and resumes created by the existing UUID-v4 run allocator retain their tracker layout and durable identity. Deliberately incompatible non-canonical or non-UUID-v4 `resume_run_id` values are rejected before repository preflight, tracker I/O, or background job registration. Resume tracker layouts containing symlinks or non-directory/non-file nodes are rejected before tracker snapshot, open, or access; in background mode, that layout validation occurs after generic job registration, so no pre-job-rejection claim is made for this layout class. No model-owned cross-repository authorization route was added.
- **Rollback:** if this containment change must be withdrawn, revert `6a14a7a250f93a710d536b9648c4a89a511d4208`, then `32895e077da7b34b2099fb7c8aa884a1c15b865f`, then `2dfb9aefab75383ba18dfe660966b0d2bd4f748a`, then `c32ef8675a3e47a8672bebb1e5fd12b74ab58ada`, then `7931d4a03972c8e4c84aacb35c3a6993773130e4`, then `342535bfd845daf17a0b1c17f9086ed350b13d38`, then `e05740d0c559f484286d473986511f6bd718d83f`, then `75b13a81cff24f44637d605e4e715cd09ca20bfe` in reverse dependency order, and revert the corresponding later tracker-only accounting commit separately. `d9942da3a3f695b923c5113bc336554b190061f9` is the predecessor durable planning checkpoint and is not part of the implementation rollback sequence. Partial rollback is unsafe because the later commits close ordinary-startup-failure classification, registered-startup settlement, pending-preparation cleanup, pending-startup cancellation, post-preflight cancellation, preflight-order, and resume tracker-identity gaps in the first implementation; prefer a complete reverse-order rollback or a focused roll-forward correction.
- **Residual non-blocking risks:** canonicalization and contained state traversal do not claim an OS-level, race-free filesystem capability against a privileged concurrent attacker after validation; repository containment does not grant or isolate every other Host effect class; and hostile-candidate evaluator isolation plus executable/runtime/image identity remain the separately scoped concerns listed above. None expands issue #1's required canonical target and pre-effect rejection boundary or blocks its closure after the Chunk 01 accounting commit, aggregate current-tree verification and review, and the later GitHub comment/read-back/closure and terminal accounting actions.
- **Tracker-only accounting evidence:** record predecessor durable checkpoint `d9942da3a3f695b923c5113bc336554b190061f9`; implementation commit `75b13a81cff24f44637d605e4e715cd09ca20bfe`; corrective commits `e05740d0c559f484286d473986511f6bd718d83f`, `342535bfd845daf17a0b1c17f9086ed350b13d38`, `7931d4a03972c8e4c84aacb35c3a6993773130e4`, `c32ef8675a3e47a8672bebb1e5fd12b74ab58ada`, `2dfb9aefab75383ba18dfe660966b0d2bd4f748a`, `32895e077da7b34b2099fb7c8aa884a1c15b865f`, and `6a14a7a250f93a710d536b9648c4a89a511d4208`; their exact changed paths, including exactly `src/controller.ts`, `tests/autoresearch.spec.ts`, and `tests/controller.spec.ts` for the pending-prepare correction and exactly `src/index.ts` and `tests/autoresearch.spec.ts` for both the registered-startup and ordinary-startup-failure corrections; the latest observed green build, exact five-file **305-test (305/305)** focused Vitest result, and typecheck result; all review and re-review findings/corrections; the latest clean implementation review; compatibility, rollback, and residual non-blocking risks.
- **Accounting review result:** the Chunk 01 staged tracker-only accounting diff was independently reviewed before commit. Review found that the durable resume pointer still named `Chunk 01 — accounting review` and that the pre-commit accounting-review checkbox remained unchecked; both tracker-only findings were corrected before commit, with no claim that this accounting commit has already landed.
- **Current resume pointer:** `resume at Chunk 02 — fresh current-tree verification/accounting`.
- [x] Scoped implementation complete.
- [x] Fresh verification passes on the current tree.
- [x] Issue-specific review/fix loop is clean.
- [x] Accounting record complete and staged tracker-only diff reviewed for commit.

### Chunk 02 — Freshly verify and account issue #2 Host/human-owned evaluator authority

- **Issue owner:** #2 only.
- **Predecessor durable checkpoint:** `367b7d5e89f331bcc541c280f0dac2bf3cf9bb26` (`docs(plan): record issue 1 containment`) durably records the completed Chunk 01 implementation, verification, review, and accounting state that precedes this issue #2 assessment.
- **Current-tree assessment:** fresh inspection confirmed the production issue #2 Host/human-owned evaluator authority boundary. Corrective test commit `b5b74783e6021ddd09d62fc659c0b3294fcc28f0` (`test(security): cover evaluator authority drift`) added command, cwd, and environment-value drift coverage in `tests/controller.spec.ts` but displaced registration-removal, args-drift, declared-file-drift, and terminal exact-replay cases. Corrective commit `5d8d14484985731282af3df1caa56b6ba1eca52e` (`test(security): restore evaluator lifecycle coverage`) restored those cases. Final coverage review then found that the restored tests still did not exercise the complete durable-registration field matrix across both nonterminal resume and terminal replay. Corrective commit `ea90b9be3a3fac77fe33c60ce4ec4cc06de9bf76` (`test(security): cover full evaluator registration drift`) changed only `tests/controller.spec.ts`, covering the full matrix in both lifecycle modes and keeping registration removal as a separate case; no production source change was required.
- **Production evidence — public boundary and ordering:** the strict start/resume union at `src/types.ts:199-266` requires `run_tag` plus opaque `evaluator_id` for a new start, permits `resume_run_id` while rejecting `evaluator_id` on resume, and rejects unknown/raw keys. `ACTIVATION_AUTORESEARCH_TOOL_SCHEMA` at `src/types.ts:224-241` exposes no command, args, cwd, environment, registration content, metric, provenance, or exceptional allowlist authority. The sole production route registers that schema and decoder at `src/index.ts:87-106`, and `validateAutoresearchRequest` completes before background repository preflight or job registration at `src/index.ts:105-128`.
- **Production evidence — Host selection and durable identity:** new-start selection resolves only the immutable deployment registry ID before repository work at `src/controller.ts:38-44,178-190`. Host registrations are normalized, sorted, deeply frozen, exact-key validated, and resolved by ID at `src/config.ts:193-230`; command, args, cwd, and environment originate there rather than in model input. New runs derive policy from that Host registration and atomically persist the full registration, manifest, and fingerprint at `src/controller.ts:317-336` and `src/tracker.ts:172-182`. The fingerprint binds contract generation, evaluator ID, canonical command/args/cwd/environment and registration content, plus the manifest at `src/types.ts:96-132`.
- **Production evidence — resume and recovery:** resume derives evaluator identity from durable state, resolves the current Host registration by durable evaluator ID, and rejects removal or semantic registration/fingerprint drift before writable ownership at `src/controller.ts:192-265`, with a second durable identity check after writable open at `src/controller.ts:261-264`; decoding prevents any submitted resume evaluator ID at `src/types.ts:246-266`. Recovery receives the same `DurableRegistrationIdentity` at `src/recovery.ts:15` and classifies current versus legacy/corrupt authority before recovery at `src/recovery.ts:45-60`. Controller recovery/replay passes that identity at `src/controller.ts:247-250,288-301,395-401`: current-contract corruption or mismatch blocks, legacy nonterminal runs remain read-only blocked, legacy terminal replay is validation-only, and no migration or fallback synthesizes current Host authority.
- **Production evidence — spawn and public output:** every evaluator spawn uses the durable policy produced from the accepted registration at `src/controller.ts:496-530`. `runEvaluator` rebinds evaluation to the captured boundary and spawns exactly `[command, ...args]`, the canonical cwd, and a closed explicit environment at `src/evaluator.ts:253-290,315-325,483-489`; it does not inherit the parent environment. Canonical public-result exact-key decoders at `src/types.ts:577-660` and renderer fields at `src/render.ts:12-42` expose no evaluator registration, argv, cwd, or environment.
- **Exact existing test evidence:** omitted start evaluator ID, submitted resume evaluator ID (`matching`, `mismatching`, and `unknown`), removed raw authority keys, and public-schema absence are covered at `tests/contracts.spec.ts:298-307`; deterministic/sorted Host registry, unknown ID, reserved environment, unknown registration content, and duplicate IDs at `tests/contracts.spec.ts:268-285`; loader/runtime config parity at `tests/contracts.spec.ts:288-296`; unknown ID before Git/repository mutation at `tests/controller.spec.ts:53-59`; unknown background ID before preflight/job/controller at `tests/autoresearch.spec.ts:299-304`; production schema/guidance absence of raw authority at `tests/autoresearch.spec.ts:130-158`; and registration-derived argv/metric/environment/provenance at `tests/autoresearch.spec.ts:123-127`.
- **Exact durable/recovery test evidence:** atomic durable registration identity is covered at `tests/controller.spec.ts:467-480`; strict durable JSON/fingerprint/presence checks at `tests/tracker.spec.ts:536-588`; corrupt durable registration before spawn/mutation at `tests/controller.spec.ts:511-527,610-645` and `tests/recovery.spec.ts:191-228`. Corrective commit `ea90b9be3a3fac77fe33c60ce4ec4cc06de9bf76` adds a shared matrix in `tests/controller.spec.ts` that exercises, separately for nonterminal resume and terminal replay, drift in command, args, cwd, environment value, environment key, environment set addition, environment set removal, `metricName`, `metricDirection`, `evaluatorFiles`, local-dataset files, local-dataset identity, local-dataset kind, external-dataset digest, external-dataset identity, and external-dataset kind. Host registration removal remains a separate rejection case in each lifecycle mode. Every rejection asserts the expected mismatch result while preserving tracker bytes and transition count, claims/authority state, Git HEAD/refs/status, child count, and evaluator-spawn count. Successful terminal exact-registration replay remains separately covered and permits authority-inspection bookkeeping updates while preserving no claims or run transitions, Git activity, child activity, or extra evaluator spawn. Environment inclusion in the canonical registration/fingerprint, with value drift significant and key/manifest reordering stable, remains covered at `tests/tracker.spec.ts:536-551`.
- **Exact spawn/lifecycle/public test evidence:** exact spawned argv/cwd/environment is covered at `tests/evaluator.spec.ts:289-300`; raw environment reaching only spawn and remaining hashed/redacted elsewhere at `tests/evaluator.spec.ts:319-336`; argv/cwd matching the frozen policy boundary at `tests/evaluator.spec.ts:583-588`; public interruption resume, descendant evaluator cancellation/terminal replay, and proven-quiescent rerun at `tests/restart.integration.spec.ts:92-175`; and independent public/release absence of removed authority fields at `tests/release.spec.ts:140-156`.
- **Final coverage finding, correction, and review result:** re-review first found that `b5b74783e6021ddd09d62fc659c0b3294fcc28f0` displaced existing lifecycle cases; `5d8d14484985731282af3df1caa56b6ba1eca52e` restored them. The resumed coverage review then found that the resulting suite still split a partial authority-drift set from restored cases and did not prove every registration field across both nonterminal and terminal lifecycle paths. Corrective commit `ea90b9be3a3fac77fe33c60ce4ec4cc06de9bf76` (`test(security): cover full evaluator registration drift`) changed only `tests/controller.spec.ts`, consolidated the complete 16-field registration-drift matrix across nonterminal resume and terminal replay, retained registration removal as an explicit separate case, and preserved successful terminal exact-replay coverage. The final code/ownership re-review traced deployment-owned selection through durable persistence, recovery, replay, and every spawn; it returned zero findings, so the Chunk 02 issue-specific review/fix loop is complete.
- **Latest observed verification after the final coverage correction:** `pnpm run build` passed; exact command `pnpm exec vitest run tests/contracts.spec.ts tests/autoresearch.spec.ts tests/controller.spec.ts tests/tracker.spec.ts tests/recovery.spec.ts tests/evaluator.spec.ts tests/restart.integration.spec.ts tests/release.spec.ts` passed **8 files / 448 tests (448/448)**; `pnpm run typecheck` passed.
- **Historical remediation evidence:** preparation `cf3d0c33d24902fb558500dfcd176d23586c97a1` established the Host evaluator registry and model contract; durable-registration preparation `174ecb01d2834398c9b0c2496c0f53a679ed5730` established durable evaluator registration identity, with `efaabef4da68387c417371a4a8611f2b6bb60667` serving as its tracker/accounting commit; activation `4b9e93efcb343b9a9cf017ba47605dc44e867e6a`, activation accounting `0dee00d206dd4414c4034255c0affd5a2050b730`, and trust-boundary documentation `aec50bc625d1635297f83643d00834adfb194744` remain preserved above. Fresh current-tree evidence, rather than history alone, proves the implementation state recorded here.
- **Compatibility and residual non-blocking concerns:** all three corrective commits are test-only changes to `tests/controller.spec.ts`; they do not alter the public request/result contract, durable schema, production registration canonicalization, resume/recovery behavior, or spawn behavior. Exact-registration terminal replay remains compatible, while registration removal and every semantically changed canonical field remain deliberately rejected before writable effects. Environment and manifest key reordering that is canonically equivalent remains compatible. Hostile candidate-code OS/filesystem/process/network/credential isolation remains broader separately tracked work and is not required for issue #2 closure after evaluator argv/environment selection is Host/human-owned. Metric-definition ownership likewise remains outside issue #2 and cannot trigger remediation or block closure.
- **Rollback:** if the Chunk 02 test corrections must be withdrawn, revert `ea90b9be3a3fac77fe33c60ce4ec4cc06de9bf76`, then `5d8d14484985731282af3df1caa56b6ba1eca52e`, then `b5b74783e6021ddd09d62fc659c0b3294fcc28f0` in reverse dependency order. The eventual Chunk 02 tracker-only accounting commit is reverted separately. These reversions remove only the current Chunk 02 coverage and accounting record; production evaluator-authority behavior and all historical production remediation commits remain untouched.
- **Latest implementation review result:** clean; the final issue-specific code/ownership review returned zero findings after retracing the public request boundary, deployment-owned registration selection, durable identity and mismatch rejection, recovery and terminal replay behavior, and exact spawn authority. No model field, candidate content, resume input, migration, or recovery fallback can alter evaluator selection, command, argv, cwd, environment, metric fields, evaluator-file declarations, or dataset declarations. Successful terminal exact replay preserves no claims/run transitions, Git activity, child activity, or extra spawn while permitting authority-inspection bookkeeping updates. Issue #2 remains open pending Chunk 02 tracker-only accounting, later aggregate current-tree verification and review, and the issue-specific GitHub actions and terminal accounting.
- **Accounting review result:** an independent review of the staged Chunk 02 tracker-only accounting diff found that the historical preparation commit was abbreviated as `cf3d0c3`, the durable resume pointer still named the Chunk 02 accounting review, and the pre-commit accounting-review checkbox remained unchecked. All three tracker-only findings were corrected before commit; this record does not claim that the Chunk 02 accounting commit has landed.
- **Current resume pointer:** `resume at Chunk 03 — fresh current-tree verification/accounting`.
- [x] Scoped implementation is present on the current tree; final corrective coverage commit `ea90b9be3a3fac77fe33c60ce4ec4cc06de9bf76` landed only in `tests/controller.spec.ts` after `b5b74783e6021ddd09d62fc659c0b3294fcc28f0` and `5d8d14484985731282af3df1caa56b6ba1eca52e`.
- [x] Fresh current-tree build, exact eight-file Vitest verification (**448/448**), and typecheck pass after corrective commit `ea90b9be3a3fac77fe33c60ce4ec4cc06de9bf76`.
- [x] Issue-specific ownership review/fix loop is clean.
- [x] Accounting record complete and staged tracker-only diff reviewed for commit.

### Chunk 03 — Freshly verify and account issue #3 declared evaluator/dataset bytes

- **Issue owner:** #3 only.
- **Predecessor durable checkpoint:** `967012e271eaf1b912550e0ef6b826b24897341b` (`docs(plan): record issue 2 evaluator authority`) durably records the completed Chunk 02 accounting state that precedes this issue #3 review/fix sequence.
- **Work:** inspect the current production boundary and preserve explicit Host declarations for evaluator files and local dataset files; derive identity and SHA-256 from the isolated start commit before baseline; persist the exact manifest; automatically exclude every declared path from the mutable surface regardless of mutable globs or filename; revalidate on baseline, every candidate, resume, and recovery before spawn. Retain attempt-local anti-swap checks where implemented. Do not add an external-dataset digest contract to #3 verification, remediation, or closure work.
- **Verification:** the issue’s representative `bench/score.mjs` and `data/holdout.json` cases plus broad mutable globs, missing file, symlink, rename, content replacement, declaration drift, manifest corruption, inode/device swap, accepted-candidate resume, recovery rerun, and mismatch rejection before spawn. Confirm protection is declaration-driven, never based on `eval`/`evaluator`/`dataset` filename heuristics.
- **Public wording review:** claims must say the **declared evaluator and dataset files** are content-bound/frozen and revalidated, not that the executable, interpreter, dependency graph, provider, or image is frozen.
- **Implementation policy:** if fresh verification finds no scoped defect, make no source/test change; account the current evidence. If it finds one, land one focused corrective commit and rerun the full scoped verification/review.
- **Tracker-only accounting commit:** record historical SHAs cited, current-tree evidence, any corrective SHA, exact paths/results, review, rollback, and the separately tracked runtime-dependency risk; stage and review before commit.
- **Review findings:** issue-specific review invalidated the earlier no-defect assessment. The local dataset was omitted from the immediate pre-provider-spawn and post-provider-spawn frozen-boundary checks, so evaluator files were revalidated at the final spawn boundary while local-dataset bytes were not. Coverage also lacked resume/recovery lifecycle cases that mutate declared local-dataset bytes immediately before provider spawn and prove rejection before the provider is invoked.
- **Corrective commit:** `f4bf89f83d141b1f087689068badbe89d7e83df1` (`fix(security): revalidate dataset before evaluator spawn`) changed exactly four paths: `src/controller.ts`, `src/evaluator.ts`, `tests/controller.spec.ts`, and `tests/evaluator.spec.ts`. The correction carries the declared local-dataset manifest into the immediate pre/post-provider spawn revalidation boundary and adds the missing resume/recovery byte-mutation lifecycle coverage, including rejection before provider invocation.
- **Historical remediation roles and accounting:** `75949d0fe7aa504537e98b5941e14f7da364279e` prepared frozen evaluator/local-dataset inputs, with historical tracker checkpoint `b2a7f14d2e3f8bdaa375d659a02abbb50564db28`; `cf3d0c33d24902fb558500dfcd176d23586c97a1` separately prepared the Host evaluator registry and declaration/model contract, with accounting checkpoint `488c73a1edb43671730ca8dbe5f257f8f8c461db`; `174ecb01d2834398c9b0c2496c0f53a679ed5730` prepared durable evaluator-registration identity, with checkpoint `efaabef4da68387c417371a4a8611f2b6bb60667`; activation `4b9e93efcb343b9a9cf017ba47605dc44e867e6a` and its checkpoint `0dee00d206dd4414c4034255c0affd5a2050b730` made the Host-owned frozen-input boundary live. The intervening Chunk 03R hardening sequence is paired exactly as follows: `d9c10369cdc83948dd6cd0960fe2d63caf1d2fac` hardened shared activation primitives, including evaluator manifest reconstruction and frozen-file handling; `da87f21276a83f842cee15f6870818caada71751` corrected durable-evidence-preserving read-only tracker snapshot validation; and `aca459bb94a895078cda855f64c8fde5e7e356e5` aligned activation-schema contract parity. Those three commits are accounted by `5df192445a6d2da96958590d3d4c00d8f1b015a9` (`docs(plan): record handoff chunk 03R`). The reopened Chunk 03R review fixes `653add91657902e3e4889796da510dbdb1d191b9` preserved claimed terminal Git authority and `9d6b3ca503fa27256095765a1dbdc807188c6689` fenced terminal-lock recovery; those two commits are accounted by `ed849ad18279e14e54ac69072b6e32b5873e3df9` (`docs(plan): record chunk 03R authority fix`). For issue #3, only the frozen-input/manifest portions of `d9c10369cdc83948dd6cd0960fe2d63caf1d2fac` are relevant historical preparation; the read-only tracker snapshot, activation-schema parity, terminal-authority, and terminal-lock-recovery portions are unrelated to issue #3 and are recorded here solely to delimit the complete historical hardening/accounting sequence. `f511456436d2067a522e8e001f49dd8bdeb13603` corrected the typed frozen-boundary failure contract, and `aec50bc625d1635297f83643d00834adfb194744` aligned the earlier README trust-boundary wording. The typed-boundary correction and earlier README closure are recorded by terminating historical checkpoint `6cbe16c0a702c2976a4d5401d3d0fdf34389cfdc` (`docs(plan): record handoff chunk 08`).
- **README review finding and correction:** re-review found that `README.md` did not describe the immediate pre/post-provider-spawn checks precisely enough after the local-dataset correction. Corrective commit `676dad082c93edf49675b4788a7ed768f1480afd` (`docs(security): clarify frozen input checks`) changed only `README.md` to state the scoped declared-file checks without expanding the guarantee to runtime dependencies, the provider, or isolation. This is documentation evidence only; it does not claim a further runtime fix.
- **Latest observed verification after the correction:** `pnpm run build` passed; exact command `pnpm exec vitest run tests/git.spec.ts tests/evaluator.spec.ts tests/controller.spec.ts tests/recovery.spec.ts tests/tracker.spec.ts tests/contracts.spec.ts tests/restart.integration.spec.ts tests/release.spec.ts` passed **8 files / 454 tests (454/454)**; `pnpm run typecheck` passed.
- **Compatibility:** the public request/result contract, durable tracker schema, Host-owned declaration/registration model, manifest format, and ordinary start/resume/recovery behavior remain unchanged. The runtime correction only strengthens enforcement at the existing provider-spawn boundary, and the README correction only clarifies that boundary. Runs whose declared evaluator and local-dataset bytes remain unchanged continue normally; byte mutation at that boundary is deliberately incompatible and now fails closed before provider execution.
- **Rollback:** if the runtime correction must be withdrawn before Chunk 03 accounting, revert `f4bf89f83d141b1f087689068badbe89d7e83df1`; doing so knowingly restores the local-dataset pre/post-provider-spawn gap and removes its resume/recovery regression coverage, so do not describe issue #3 as satisfied at that reverted tip. If only the README clarification must be withdrawn, revert `676dad082c93edf49675b4788a7ed768f1480afd`; doing so restores imprecise public wording without changing runtime enforcement. If both are withdrawn, revert the README correction first and the runtime correction second. The eventual tracker-only Chunk 03 accounting commit is reverted separately. Historical frozen-input, registration, activation, typed-boundary, README, and accounting commits remain untouched.
- **Scope arbitration and residual non-blocking risks:** a runnable child process executing under the same UID could race declared-file revalidation and use (a TOCTOU/runtime-isolation concern). That concern is legitimate and should be tracked separately, but issue #3 explicitly requires identity/SHA-256, mutable-surface exclusion, and revalidation before each spawn; it does not require an OS-enforced race-free capability or isolation from a same-UID runnable child after the pre-spawn acceptance check. Neither `f4bf89f83d141b1f087689068badbe89d7e83df1` nor `676dad082c93edf49675b4788a7ed768f1480afd` claims to fix that concern, and it is non-blocking for issue #3. Public claims remain limited to declared evaluator and local-dataset files, their start-commit hashes, durable manifest, mutable-surface exclusion, and revalidation. They do not claim closure over the executable, interpreter, transitive dependency graph, provider implementation, runtime, container/image, hostile-code filesystem/process/network effects, runtime/image dependency identity, or external-dataset digest or availability.
- **Final Chunk 03 reviews:** the final issue-specific code review of the corrected current-tree file-identity boundary found no unresolved issue: declared evaluator and local-dataset bytes are covered at the immediate pre/post-provider-spawn boundary, including the resume/recovery mutation cases, and the separately recorded TOCTOU/runtime-isolation concern does not expand issue #3. The final README wording review likewise found no unresolved issue: the text accurately limits the guarantee to declared evaluator and dataset files and does not claim frozen executables, dependencies, providers, runtimes, images, hostile-code isolation, or an external-dataset digest contract.
- **Accounting review result:** an independent review of the staged Chunk 03 tracker-only accounting diff found that the Host registry/declaration preparation commit and its distinct accounting checkpoint were omitted, the durable resume pointer still named the Chunk 03 accounting review, and the pre-commit accounting-review checkbox remained unchecked. All three tracker-only findings were corrected before commit; this record does not claim that the Chunk 03 accounting commit has landed.
- **Current resume pointer:** `resume at Chunk 04 — aggregate current-tree verification and review`.
- [x] Scoped corrective implementation landed as `f4bf89f83d141b1f087689068badbe89d7e83df1` in exactly the four recorded paths.
- [x] Fresh current-tree build, exact eight-file Vitest verification (**454/454**), and typecheck pass after the corrective commit.
- [x] Issue-specific file-identity review/fix loop is clean.
- [x] Public wording is exact and truthful.
- [x] Accounting record complete and staged tracker-only diff reviewed for commit.

### Chunk 04 — Aggregate review, issue comments, and closure accounting

- **Clean entry path:** enter after Chunks 00–03 are complete and accounted with no unresolved in-scope blocker; perform aggregate verification/review, then the issue actions and terminal accounting below.
- **Blocker-mode entry path:** when any Chunk 01–03 has a genuine in-scope blocker, enter after all reachable work and its blocker record are accounted. Skip only aggregate prerequisites that the blocker makes impossible. Prepare and process issue-specific blocker comments, independently close every satisfied issue, leave blocked issues open, perform the post-action state read-backs, and publish terminal accounting with one canonical aggregate resume pointer.
- **Aggregate verification (clean mode, and reachable portions in blocker mode):** run the repository’s final required checks and smoke path, but credit only observed results. Review the complete implementation/accounting range and resolve every reachable in-scope finding before preparing issue actions.
- **Final split review finding and job-cleanup correction:** the aggregate split review found that cancellation returned the prompt result before abortable controller preparation cleanup completed, allowing the registered job's `done` lifecycle and harness teardown to settle out of cleanup order. Corrective commit `8f30f624f604a21f9c60599012b0ee5468c6ae9d` (`fix(jobs): await cancelled preparation cleanup`) changed exactly `src/controller.ts`, `tests/autoresearch.spec.ts`, and `tests/controller.spec.ts`. Disposal is now idempotent, awaits an in-flight preparation promise, and releases any runtime produced by that preparation before disposal, registered-job completion, or teardown settles; focused coverage proves prompt cancellation remains prompt while lifecycle completion waits for cleanup.
- **Generated-release descendant-marker cleanup:** aggregate release review found that the packed-release interruption scenario could leave its generated descendant marker behind. Corrective commit `7abc48e7e01c764b5eb5b8f2e731f901d2a55107` (`test(release): clean descendant markers`) changed only `tests/release-scenarios.integration.spec.ts` and removes that marker in the scenario's `finally` cleanup after harness disposal.
- **Compatibility:** `8f30f624f604a21f9c60599012b0ee5468c6ae9d` preserves the public request/result contract and prompt cancellation response while deliberately strengthening lifecycle ordering: registered-job completion and harness teardown now wait for abortable preparation cleanup. `7abc48e7e01c764b5eb5b8f2e731f901d2a55107` changes test-generated artifact cleanup only and does not alter production or packaged behavior.
- **Rollback:** if the job-cleanup correction must be withdrawn, revert `8f30f624f604a21f9c60599012b0ee5468c6ae9d`; doing so knowingly restores premature registered-job/teardown settlement while preparation cleanup remains pending. If only the generated-marker correction must be withdrawn, revert `7abc48e7e01c764b5eb5b8f2e731f901d2a55107`; doing so restores release-scenario marker leakage but no production behavior. Revert any later Chunk 04 tracker-only accounting commit separately.
- **Residual non-blocking risks:** cancellation still depends on underlying preparation operations honoring their abort signal and eventually completing cleanup; a permanently non-cooperative operation can therefore keep lifecycle completion and teardown pending, rather than falsely reporting cleanup complete. The marker correction covers the generated descendant marker owned by the release scenario; abrupt process termination outside `finally` can still leave ordinary temporary test artifacts for external cleanup. Neither risk changes the scoped #1–#3 security conclusions.
- **Definitive current-HEAD aggregate gates:** `pnpm run check` passed **11 files + 1 skipped**, **522 passed + 7 skipped (529 total)**; `pnpm peers check` completed with **no issues**; and `pnpm run release:smoke` returned `ok: true` for `dsh-autoresearch-0.1.5.tgz`.
- **Final aggregate review result:** clean. The final split reviews and the post-fix reviews of the job-cleanup ordering correction, generated release descendant-marker cleanup, complete implementation/accounting range, and current-HEAD aggregate evidence returned no unresolved in-scope findings.
- **Final corrective commits, findings, paths, and roles:** `8f30f624f604a21f9c60599012b0ee5468c6ae9d` (`fix(jobs): await cancelled preparation cleanup`) is the production lifecycle-ordering correction from the final split review; it changed `src/controller.ts`, `tests/autoresearch.spec.ts`, and `tests/controller.spec.ts` so disposal awaits in-flight abortable preparation cleanup and releases any late runtime before registered-job completion or teardown. `7abc48e7e01c764b5eb5b8f2e731f901d2a55107` (`test(release): clean descendant markers`) is the generated-release test-artifact correction; it changed only `tests/release-scenarios.integration.spec.ts` so the interruption scenario removes its descendant marker in `finally` after harness disposal.
- **Final review closure:** Slice A, Slice B, and Slice C reviews are clean. The post-fix reviews of both final corrective commits and the final completeness review are also clean, with no unresolved in-scope technical or accounting finding.
- **Approved issue-specific comments:** the separate Chinese closure comment for each of issues #1, #2, and #3 was independently reviewed against its issue body, implementation/accounting evidence, current-HEAD gates, residual non-blocking risks, and intended close action, and each was approved. `local://approved-issue-comments.json` is transient context only, not a durable source of truth. If that artifact is unavailable on resume, regenerate all approved bodies from this tracker and the then-current canonical issue state, and independently re-review each regenerated body before posting.
- **External blocker:** GitHub issue #1 was re-read as **open and unchanged** at **2026-09-02T16:36:16Z**. `gh auth status` showed no authenticated session. A device-auth attempt could not complete without user authentication. Consequently, no comment was posted and no close action occurred for #1. Sequential processing stopped there: issues #2 and #3 were not attempted and remain open. This is an external execution blocker only; all reachable implementation, verification, and review work is complete.
- **Canonical resume pointer:** `authenticate GitHub CLI/API, then resume at Chunk 04 issue #1 pre-post state read and post the already approved #1 comment; continue #2/#3 sequentially, close/read back, then terminal accounting`.
- **Terminal accounting status:** not complete. This tracker-only blocked-state record truthfully prepares the external-action resume state; the terminal accounting commit remains prohibited until every comment post/read-back, close, and mandatory post-action canonical state read-back has completed.
- [x] Aggregate technical verification is complete at the definitive current HEAD: `pnpm run check` passed **11 files + 1 skipped**, **522 passed + 7 skipped**; `pnpm peers check` was clean; and release smoke returned `ok: true` for version `0.1.5`.
- [x] Final Slice A/B/C, post-fix, and completeness reviews are clean; all three issue-specific Chinese comment bodies were independently reviewed and approved.
- [ ] External comment pre-post state read and posting complete for #1. **Blocked:** issue re-read open and unchanged at `2026-09-02T16:36:16Z`, but no authenticated GitHub CLI/API session was available; no comment was posted.
- [ ] External comment read-back complete for #1, including canonical comment URL, timestamp, exact rendered body, and then-current issue state/reason. **Blocked:** no #1 comment exists because authentication did not complete.
- [ ] External close action and mandatory post-action canonical state read-back complete for #1. **Blocked:** no close action occurred; #1 remains open.
- [ ] External comment pre-post state read, posting, and comment read-back complete for #2. **Blocked:** sequential processing stopped at #1; #2 was not attempted and remains open.
- [ ] External close action and mandatory post-action canonical state read-back complete for #2. **Blocked:** #2 was not attempted and remains open.
- [ ] External comment pre-post state read, posting, and comment read-back complete for #3. **Blocked:** sequential processing stopped at #1; #3 was not attempted and remains open.
- [ ] External close action and mandatory post-action canonical state read-back complete for #3. **Blocked:** #3 was not attempted and remains open.
- [x] Blocked-state record complete and staged tracker-only diff reviewed for the blocker commit; this does not mark terminal accounting complete.
- [ ] Terminal accounting record complete and staged tracker-only diff reviewed for commit. **Blocked:** external comment/read-back/close/post-action gates remain incomplete; this is only the truthful tracker-only blocked-state record.

## Genuine in-scope blocker and resume protocol

A blocker is valid only when it prevents an explicit requirement inside the scoped remedy above. The external Host effect grant, hostile-candidate isolation provider, and executable/runtime/image dependency identity are not valid blockers for #1–#3.

For a genuine blocker:

1. Finish and account all reachable in-scope work.
2. Prepare a tracker-only blocker record naming the issue owner, exact missing in-scope prerequisite, evidence/source inspected, completed and accounted predecessor SHA range, remaining reachable work, residual risk, and exactly one pointer in the form **`resume at Chunk NN — <named step>`**.
3. Review/fix the staged tracker-only blocker diff before committing `docs(plan): record issue N in-scope blocker`.
4. Enter Chunk 04 through its blocker-mode path. Prepare the issue-specific blocker comment with the same evidence and resume pointer; apply the pre-post review, comment post/read-back, correction, issue-state action, and mandatory post-action canonical state gates.
5. In terminal accounting, publish one canonical aggregate pointer to the earliest blocked in-scope step; it supersedes individual pointers without erasing them. Terminal accounting is prohibited until every issue's post-action canonical state/reason/timestamp has been read back successfully. Never use a vague pointer such as “resume security work.”

Expected pointers if a blocker occurs in this plan:

- #1: `resume at Chunk 01 — canonicalize and compare the requested target before repository discovery`.
- #2: `resume at Chunk 02 — trace immutable Host/human evaluator registration through the first failing lifecycle path`.
- #3: `resume at Chunk 03 — revalidate the declared evaluator/dataset manifest before the first failing spawn path`.
- Issue-action/accounting only: `resume at Chunk 04 — pre-post review of the first unposted or uncorrected issue comment`.

## Final completion criteria

- [x] #1 enforces canonical parent repository/workspace containment before effects; cross-repository authority is separate and non-model-owned, or cross-repository operation is rejected.
- [x] #2 proves immutable Host/human ownership of evaluator selection, argv, and environment through start, resume, recovery, and spawn.
- [x] #3 proves declared evaluator/dataset identity, SHA-256, automatic exclusion, and pre-spawn revalidation through baseline, candidate, resume, and recovery.
- [x] Historical remediation is preserved and the technical closure assessment relies on fresh current-tree verification and issue-specific review.
- [x] Broader effect authority, hostile-candidate isolation, and runtime dependency closure are separately recorded and explicitly non-blocking.
- [ ] Every tracker-only accounting or blocker record is complete and its staged diff is reviewed before commit; landed accounting SHAs are carried forward by the durable checkpoint protocol without self-reference. **Pending only for terminal accounting:** this blocked-state record is complete and reviewed, but the eventual terminal accounting record cannot be completed until the external gates finish.
- [ ] Every external comment passes its then-current pre-post state review, is posted and read back, and is explicitly corrected and re-accounted if inaccurate. **Blocked:** approved bodies exist only as transient context until authenticated sequential posting begins; no external comment action occurred.
- [ ] After every close action, the canonical issue is read back again; final state, state reason, UTC timestamp, canonical URLs, comment URLs, SHAs, evidence, residual risks, rollback, and the exact aggregate resume pointer are durably accounted. **Blocked:** #1 remains open after authentication failure; #2/#3 were not attempted and remain open; terminal accounting must wait.
